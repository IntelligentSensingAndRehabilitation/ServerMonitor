# AUTO-GENERATED FILE - DO NOT EDIT MANUALLY
# This file is generated by generate_alerts.py
# To make changes, edit config/alerts_config.json and run: python3 generate_alerts.py

groups:
- name: physical_drives
  interval: 1m
  rules:
  - record: fs:physical:size_bytes
    expr: max by (hostname, instance, device, mountpoint) (node_filesystem_size_bytes{fstype=~"ext4|ext3|xfs|btrfs|zfs",mountpoint!~"/(boot|snap|run|dev|proc|sys|var/lib|autofs)(|/.*)"})
  - record: fs:physical:avail_bytes
    expr: max by (hostname, instance, device, mountpoint) (node_filesystem_avail_bytes{fstype=~"ext4|ext3|xfs|btrfs|zfs",mountpoint!~"/(boot|snap|run|dev|proc|sys|var/lib|autofs)(|/.*)"})
  - record: fs:physical:used_bytes
    expr: |-
      max by (hostname, instance, device, mountpoint) (node_filesystem_size_bytes{fstype=~"ext4|ext3|xfs|btrfs|zfs",mountpoint!~"/(boot|snap|run|dev|proc|sys|var/lib|autofs)(|/.*)"})
        - max by (hostname, instance, device, mountpoint) (node_filesystem_avail_bytes{fstype=~"ext4|ext3|xfs|btrfs|zfs",mountpoint!~"/(boot|snap|run|dev|proc|sys|var/lib|autofs)(|/.*)"})
  - record: fs:physical:used_percent
    expr: |-
      100 * (
          1 - max by (hostname, instance, device, mountpoint) (node_filesystem_avail_bytes{fstype=~"ext4|ext3|xfs|btrfs|zfs",mountpoint!~"/(boot|snap|run|dev|proc|sys|var/lib|autofs)(|/.*)"})
          / max by (hostname, instance, device, mountpoint) (node_filesystem_size_bytes{fstype=~"ext4|ext3|xfs|btrfs|zfs",mountpoint!~"/(boot|snap|run|dev|proc|sys|var/lib|autofs)(|/.*)"})
        )
- name: storage_projection
  interval: 5m
  rules:
  - record: fs:physical:growth_bytes_per_day_7d
    expr: |-
      (
          avg_over_time(fs:physical:used_bytes[1d])
          - avg_over_time(fs:physical:used_bytes[1d] offset 7d)
        ) / 7
  - record: fs:physical:growth_bytes_per_day_28d
    expr: |-
      (
          avg_over_time(fs:physical:used_bytes[1d])
          - avg_over_time(fs:physical:used_bytes[1d] offset 28d)
        ) / 28
  - record: fs:physical:days_until_full
    expr: |-
      clamp_max(
          avg_over_time(fs:physical:avail_bytes[1d]) / (fs:physical:growth_bytes_per_day_7d > 0),
          730
        )
